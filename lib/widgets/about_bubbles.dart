import 'package:flutter/material.dart';

class AboutBubblesWidget extends StatelessWidget {
  const AboutBubblesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('AboutBubblesWidget')),
    );
  }
}
