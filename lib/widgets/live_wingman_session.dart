import 'package:flutter/material.dart';

class LiveWingmanSessionWidget extends StatelessWidget {
  const LiveWingmanSessionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('LiveWingmanSessionWidget')),
    );
  }
}
