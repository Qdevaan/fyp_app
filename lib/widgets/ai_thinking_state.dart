import 'package:flutter/material.dart';

class AiThinkingStateWidget extends StatelessWidget {
  const AiThinkingStateWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('AiThinkingStateWidget')),
    );
  }
}
