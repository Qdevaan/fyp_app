import 'package:flutter/material.dart';

class PopUpsDialogsWidget extends StatelessWidget {
  const PopUpsDialogsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('PopUpsDialogsWidget')),
    );
  }
}
