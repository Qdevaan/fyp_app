import 'package:flutter/material.dart';

class ConsultantAiChatWidget extends StatelessWidget {
  const ConsultantAiChatWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('ConsultantAiChatWidget')),
    );
  }
}
