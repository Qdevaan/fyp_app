import 'package:flutter/material.dart';

class VoiceEnrollmentWidget extends StatelessWidget {
  const VoiceEnrollmentWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('VoiceEnrollmentWidget')),
    );
  }
}
