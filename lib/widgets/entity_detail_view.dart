import 'package:flutter/material.dart';

class EntityDetailViewWidget extends StatelessWidget {
  const EntityDetailViewWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('EntityDetailViewWidget')),
    );
  }
}
