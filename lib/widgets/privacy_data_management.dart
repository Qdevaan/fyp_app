import 'package:flutter/material.dart';

class PrivacyDataManagementWidget extends StatelessWidget {
  const PrivacyDataManagementWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('PrivacyDataManagementWidget')),
    );
  }
}
