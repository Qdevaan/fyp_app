import 'package:flutter/material.dart';

class EntityKnowledgeGraphWidget extends StatelessWidget {
  const EntityKnowledgeGraphWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('EntityKnowledgeGraphWidget')),
    );
  }
}
